# depth-driller — 計画

スマホのブラウザで遊べる「ミスタードリラー」風の落ち物×掘り進みアクション。
GitHub Pages で公開する。

- 公開先: https://bubbleshaker.github.io/depth-driller/
- 操作: 画面下の仮想十字ボタン（方向を押す = その方向へ移動、ブロックがあれば掘る）

## 技術構成

| 層 | 選定 | 理由 |
|---|---|---|
| ビルド | Vite + TypeScript | 既存プロジェクトと同じ導線で Pages に載る |
| 描画 | Canvas 2D | グリッド矩形の描画。three.js は過剰、DOM は再描画で不利 |
| 状態 | 素の TS クラス（React なし） | 毎フレーム全状態更新なので仮想 DOM が邪魔になる |
| テスト | vitest | domain 層を画面なしで検証する |
| 配信 | GitHub Actions → Pages | 既存と同じ |

## レイヤ構成

```
src/
  domain/   ゲームルール。Canvas も DOM も知らない。テスト対象
    grid.ts        セル格納・座標変換・チャンク生成
    blocks.ts      ブロック種別と色
    clear.ts       同色4連結の判定と消去
    gravity.ts     支えを失ったブロックの落下
    player.ts      掘る・歩く・落ちる
    air.ts         空気ゲージ
    game.ts        上記を束ねる 1 フレーム更新
  render/   Canvas 描画のみ
  input/    タッチ/キーボード → 抽象コマンド（Direction）
  main.ts   組み立てとゲームループ
```

domain を純粋なデータ＋関数に寄せることで、連鎖消去・落下・圧死といった
「ミスタードリラーの肝」を画面なしで単体テストできる。

## マイルストーン

- [x] **M0 基盤** — Vite 雛形 / vitest / Pages デプロイの導線
- [x] **M1 掘る** — グリッド生成・プレイヤー移動・破壊・落下・圧死。ここで「遊べる」状態にする
- [x] **M2 消える** — 同色4連結の連鎖消去とスコア
- [ ] **M3 息が切れる** — 空気ゲージとエアカプセル、深度スコア、ゲームオーバー/リトライ
- [ ] **M4 スマホ最適化** — 仮想ボタン、セーフエリア、可変解像度、連打耐性
- [ ] **M5 演出** — 掘削アニメ、消去エフェクト、SE/BGM、ハイスコア保存

## ゲームルール（原作準拠の範囲）

- ブロックは 4 色。プレイヤーは上下左右に掘れる（色数は実測で決めた。summary/M2 参照）。
- 同色ブロックが 4 個以上つながると消える（消去後の落下で連鎖する）。
- 支えを失ったブロックの塊は落下する。落下ブロックに潰されると死亡。
- 空気は時間で減る。エアカプセルを取ると回復。0 で死亡。
- 深く潜るほどスコアが伸びる。深度は m 表記。
- 「×」ブロック（硬い土）は掘るのに空気を余計に消費する。

## 進め方

Issue 起票 → 実装 → reviewer サブエージェントによるレビュー → PR → マージ。
